# PostgreSQL Connection String (Neon)
# Get this from your Neon dashboard: https://console.neon.tech
DATABASE_URL=postgresql://username:password@ep-xxxxx.us-east-2.aws.neon.tech/anycomp?sslmode=require

# JWT Secret Key (use a strong random string in production)
JWT_SECRET=your_super_secret_jwt_key_change_this

# JWT Expiration Time (keep short for security: 1h, 2h, etc.)
JWT_EXPIRE=2h

# Server Port
PORT=5000

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:3000

# Node Environment
# WARNING: Set to 'production' in production to disable auto-sync
# 'development' = auto-creates tables (dangerous for production)
# 'production' = requires manual migrations (safe)
NODE_ENV=development
